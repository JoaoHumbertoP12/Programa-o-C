#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;

    printf("a > b: %d\n", a > b); // >= é maior ou igual
    printf("a < b: %d\n", a < b); // <= é menor ou igual 
    printf("a == b: %d\n", a == b); // iguais?
    printf("a != b: %d\n", a != b); // diferentes?

    return 0;
}